name: Docs Freshness

on:
  push:
    branches: [main, develop, master]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - run: bash scripts/docs-freshness-check.sh

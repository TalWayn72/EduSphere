{
  "name": "@edusphere/mcp-coordination-bridge",
  "version": "1.0.0",
  "description": "MCP server for agent coordination — pub/sub, file locks, status tracking, Iron Rule enforcement",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "bench": "vitest bench"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "better-sqlite3": "^11.0.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^3.1.0",
    "@types/better-sqlite3": "^7.6.0",
    "@types/node": "^22.0.0"
  }
}

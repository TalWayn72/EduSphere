PORT=4002
NODE_ENV=development

# Database (PostgreSQL 16 + pgvector + Apache AGE)
DATABASE_URL=postgresql://edusphere:edusphere@localhost:5432/edusphere

# Multi-tenancy
TENANT_ID=default-tenant-uuid

# CORS
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# Logging
LOG_LEVEL=debug
LOG_PRETTY=true

# ──────────────────────────────────────────────────────────────
# LTI 1.3 Integration (IMS Global LTI Advantage)
# ──────────────────────────────────────────────────────────────

# Base URL of this tool as seen by the LMS platform (no trailing slash).
# Used to build the redirect_uri sent to the platform's OIDC auth endpoint.
TOOL_BASE_URL=http://localhost:4002

# Registered LTI platform (Learning Management System) configuration.
# Obtain these values from the LMS administrator when registering EduSphere
# as an External Tool.
LTI_PLATFORM_ISSUER=https://canvas.instructure.com
LTI_PLATFORM_CLIENT_ID=your-lti-client-id-here
LTI_PLATFORM_AUTH_ENDPOINT=https://canvas.instructure.com/api/lti/authorize_redirect
LTI_PLATFORM_JWKS_URL=https://canvas.instructure.com/api/lti/security/jwks

# RSA private key (PEM format, no passphrase) used to sign tool-originated
# LTI messages (Deep Linking responses, AGS score submissions).
# Generate with: openssl genrsa -out lti_tool_private.pem 2048
# Then: export LTI_TOOL_PRIVATE_KEY=$(cat lti_tool_private.pem)
# NEVER commit the actual private key — use a secret manager in production.
LTI_TOOL_PRIVATE_KEY=

# Social types and queries are owned by the core subgraph.
# Resolvers in this subgraph are retained but not routed via SDL.
# If knowledge needs to extend SocialFeedItem or SocialRecommendation,
# use Federation @key entity extensions.

enum SocialVerb {
  COMPLETED
  ENROLLED
  ACHIEVED_BADGE
  DISCUSSED
  STARTED_LEARNING
}

# ═══════════════════════════════════════════════════════════════════════════════
# EduSphere E2E — LOCAL Development Environment
# ═══════════════════════════════════════════════════════════════════════════════
#
# Copy this file to .env.e2e.local and fill in values.
# This file is committed (example only — no secrets).
#
# Usage:
#   cp .env.e2e.local.example .env.e2e.local
#   # (no changes needed for local dev defaults)
#   pnpm --filter @edusphere/web test:e2e
#
# Prerequisites:
#   pnpm --filter @edusphere/web dev --port 5174
#   (or let Playwright start it automatically)
# ═══════════════════════════════════════════════════════════════════════════════

# ── Environment profile ─────────────────────────────────────────────────────
E2E_ENV=local

# ── Web app URL ─────────────────────────────────────────────────────────────
# Playwright's dedicated test server (separate from dev server on :5173)
E2E_BASE_URL=http://localhost:5174

# ── Backend services ────────────────────────────────────────────────────────
E2E_KEYCLOAK_URL=http://localhost:8080
E2E_KEYCLOAK_REALM=edusphere
E2E_GRAPHQL_URL=http://localhost:4000/graphql

# ── Auth mode ────────────────────────────────────────────────────────────────
# true  = auto-authenticated (no Keycloak needed, tests run fast)
# false = real Keycloak OIDC flow (requires docker-compose up)
VITE_DEV_MODE=true

# ── Test users (demo users from keycloak-realm.json) ────────────────────────
E2E_SUPER_ADMIN_EMAIL=super.admin@edusphere.dev
E2E_SUPER_ADMIN_PASSWORD=SuperAdmin123!
E2E_STUDENT_EMAIL=student@example.com
E2E_STUDENT_PASSWORD=Student123!
E2E_INSTRUCTOR_EMAIL=instructor@example.com
E2E_INSTRUCTOR_PASSWORD=Instructor123!

# ── Test behavior ────────────────────────────────────────────────────────────
# true = run mutation/write tests (default)
# false = skip write tests (safe for prod smoke runs)
E2E_RUN_WRITE_TESTS=true

# ── Output ───────────────────────────────────────────────────────────────────
E2E_SCREENSHOTS_DIR=test-results/screenshots

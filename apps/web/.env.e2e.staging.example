# ═══════════════════════════════════════════════════════════════════════════════
# EduSphere E2E — STAGING Environment
# ═══════════════════════════════════════════════════════════════════════════════
#
# Copy this file to .env.e2e.staging (gitignored — contains credentials).
# DO NOT commit the .env.e2e.staging file.
#
# Usage:
#   cp .env.e2e.staging.example .env.e2e.staging
#   # Fill in real staging URLs and credentials
#   source .env.e2e.staging && pnpm --filter @edusphere/web test:e2e
#
# Or inline:
#   E2E_ENV=staging \
#   VITE_DEV_MODE=false \
#   E2E_BASE_URL=https://staging.edusphere.io \
#   E2E_KEYCLOAK_URL=https://auth.staging.edusphere.io \
#   pnpm --filter @edusphere/web test:e2e
# ═══════════════════════════════════════════════════════════════════════════════

# ── Environment profile ─────────────────────────────────────────────────────
E2E_ENV=staging

# ── Web app URL ─────────────────────────────────────────────────────────────
E2E_BASE_URL=https://staging.edusphere.io

# ── Backend services ────────────────────────────────────────────────────────
E2E_KEYCLOAK_URL=https://auth.staging.edusphere.io
E2E_KEYCLOAK_REALM=edusphere
E2E_GRAPHQL_URL=https://api.staging.edusphere.io/graphql

# ── Auth mode (must be false for staging — real Keycloak) ───────────────────
VITE_DEV_MODE=false

# ── Test users (staging demo users) ─────────────────────────────────────────
# SECURITY: Use dedicated staging test accounts, not production credentials
E2E_SUPER_ADMIN_EMAIL=e2e.admin@staging.edusphere.io
E2E_SUPER_ADMIN_PASSWORD=REPLACE_WITH_STAGING_PASSWORD
E2E_STUDENT_EMAIL=e2e.student@staging.edusphere.io
E2E_STUDENT_PASSWORD=REPLACE_WITH_STAGING_PASSWORD
E2E_INSTRUCTOR_EMAIL=e2e.instructor@staging.edusphere.io
E2E_INSTRUCTOR_PASSWORD=REPLACE_WITH_STAGING_PASSWORD

# ── Test behavior ────────────────────────────────────────────────────────────
# Staging: run all tests including writes (creates/deletes test data)
E2E_RUN_WRITE_TESTS=true

# ── Output ───────────────────────────────────────────────────────────────────
E2E_SCREENSHOTS_DIR=test-results/staging/screenshots

/**
 * Stub for @tiptap/pm/model — separate file so Vitest assigns it a distinct
 * module-cache slot from @tiptap/pm/state and @tiptap/pm/view.
 */
export const Node = class {};
export const Fragment = class {};
export const Schema = class {};
export const Slice = class {};

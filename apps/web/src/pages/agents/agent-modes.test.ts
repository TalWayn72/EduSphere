import { describe, it, expect } from 'vitest';
import * as agentModes from './agent-modes';

describe('agent-modes', () => {
  it('exports module', () => {
    expect(agentModes).toBeDefined();
  });
});

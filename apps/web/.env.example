# Development Mode - set to 'true' to bypass Keycloak authentication
# Useful when Docker/Keycloak is not running
VITE_DEV_MODE=true

# GraphQL Gateway URLs
VITE_GRAPHQL_URL=http://localhost:4000/graphql
VITE_GRAPHQL_WS_URL=ws://localhost:4000/graphql

# Keycloak Authentication (not needed when VITE_DEV_MODE=true)
VITE_KEYCLOAK_URL=http://localhost:8080
VITE_KEYCLOAK_REALM=edusphere
VITE_KEYCLOAK_CLIENT_ID=edusphere-app

# Hive Collaboration WebSocket (Hocuspocus real-time server)
VITE_HOCUSPOCUS_URL=ws://localhost:1234

# APQ (Automatic Persisted Queries)
# Set to 'true' only when gateway has persisted-queries/manifest.json generated.
# When false (default), queries are sent as full POST bodies.
# When true, queries are sent as GET with SHA-256 hash (CDN-cacheable).
VITE_APQ_ENABLED=false

# Feature Flags (set to 'false' to disable; default is 'true' when absent)
VITE_ENABLE_PIPELINE_SUBSCRIPTIONS=true
VITE_ENABLE_NER_BRIDGE=true
VITE_ENABLE_PUBLISH_READINESS=true

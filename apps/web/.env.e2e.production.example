# ═══════════════════════════════════════════════════════════════════════════════
# EduSphere E2E — PRODUCTION Environment (READ-ONLY SMOKE ONLY)
# ═══════════════════════════════════════════════════════════════════════════════
#
# WARNING: Never run mutation/write tests against production.
# Set E2E_RUN_WRITE_TESTS=false and use --grep="smoke" filter.
#
# Copy this file to .env.e2e.production (gitignored — contains credentials).
# DO NOT commit the .env.e2e.production file.
#
# Usage (smoke only):
#   cp .env.e2e.production.example .env.e2e.production
#   # Fill in real production URLs and credentials
#   source .env.e2e.production && \
#   pnpm --filter @edusphere/web test:e2e --grep="smoke|health"
# ═══════════════════════════════════════════════════════════════════════════════

# ── Environment profile ─────────────────────────────────────────────────────
E2E_ENV=production

# ── Web app URL ─────────────────────────────────────────────────────────────
E2E_BASE_URL=https://app.edusphere.io

# ── Backend services ────────────────────────────────────────────────────────
E2E_KEYCLOAK_URL=https://auth.edusphere.io
E2E_KEYCLOAK_REALM=edusphere
E2E_GRAPHQL_URL=https://api.edusphere.io/graphql

# ── Auth mode ───────────────────────────────────────────────────────────────
VITE_DEV_MODE=false

# ── Test users (DEDICATED E2E ACCOUNTS — not real user accounts) ─────────────
# SECURITY: Use dedicated production smoke-test accounts with minimal permissions
E2E_SUPER_ADMIN_EMAIL=e2e.smoke@edusphere.io
E2E_SUPER_ADMIN_PASSWORD=REPLACE_WITH_PRODUCTION_PASSWORD
E2E_STUDENT_EMAIL=e2e.smoke.student@edusphere.io
E2E_STUDENT_PASSWORD=REPLACE_WITH_PRODUCTION_PASSWORD
E2E_INSTRUCTOR_EMAIL=e2e.smoke.instructor@edusphere.io
E2E_INSTRUCTOR_PASSWORD=REPLACE_WITH_PRODUCTION_PASSWORD

# ── Test behavior ────────────────────────────────────────────────────────────
# CRITICAL: false = skip all mutation tests in production
E2E_RUN_WRITE_TESTS=false

# ── Output ───────────────────────────────────────────────────────────────────
E2E_SCREENSHOTS_DIR=test-results/production/screenshots

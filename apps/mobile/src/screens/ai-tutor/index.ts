/**
 * ai-tutor barrel — re-exports for backward compatibility.
 */
export { default } from './AITutorScreen';
export { resolveSessionId } from './ai-tutor.types';
export { checkAiConsent, grantAiConsent } from '../../lib/ai-consent';

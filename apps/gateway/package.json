{
  "name": "@edusphere/gateway",
  "version": "0.1.0",
  "private": true,
  "description": "GraphQL Federation Gateway - Hive Gateway v2.7 (MIT-licensed)",
  "type": "module",
  "scripts": {
    "dev": "hive-gateway supergraph",
    "build": "echo 'Gateway is config-based, no build needed'",
    "compose": "hive-gateway compose",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:integration": "vitest run tests/integration",
    "lint": "echo 'No lint for config-based gateway'",
    "typecheck": "echo 'No typecheck for config-based gateway'"
  },
  "dependencies": {
    "@graphql-hive/gateway": "^1.10.0",
    "graphql": "^16.9.0"
  },
  "devDependencies": {
    "@edusphere/tsconfig": "workspace:*",
    "@types/node": "^22.10.5",
    "@vitest/coverage-v8": "^2.1.8",
    "graphql-request": "^7.1.2",
    "typescript": "^5.8.2",
    "vitest": "^2.1.8"
  }
}

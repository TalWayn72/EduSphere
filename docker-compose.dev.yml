# ═══════════════════════════════════════════════════════════════
# EduSphere - Development Docker Compose
# Local development with hot reload
# ═══════════════════════════════════════════════════════════════

version: "3.9"

services:
  # ═══════════════════════════════════════════════════════════════
  # PostgreSQL 18 + pgvector + Apache AGE (upgraded Feb 2026)
  # Fresh volumes switch automatically. Existing data: run pg_upgrade.
  # ═══════════════════════════════════════════════════════════════
  postgres:
    build:
      context: ./infrastructure/docker
      dockerfile: Dockerfile.postgres
    container_name: edusphere-postgres
    environment:
      POSTGRES_USER: edusphere
      POSTGRES_PASSWORD: edusphere_dev_password
      POSTGRES_DB: edusphere
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./infrastructure/docker/init.sql:/docker-entrypoint-initdb.d/01-init.sql
      - ./infrastructure/docker/age-setup.sql:/docker-entrypoint-initdb.d/02-age-setup.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U edusphere"]
      interval: 5s
      timeout: 5s
      retries: 5
    mem_limit: 2g
    mem_reservation: 512m
    networks:
      - edusphere

  # ═══════════════════════════════════════════════════════════════
  # PgBouncer — Connection Pooler (Port 5433 → PostgreSQL 5432)
  #
  # Pool mode: transaction — each client statement borrows a server
  # connection from the pool and releases it immediately after the
  # transaction completes. This is safe for Drizzle ORM workloads.
  #
  # WARNING: session-level features (SET LOCAL, LISTEN/NOTIFY,
  # advisory locks, temporary tables) require pool_mode=session.
  # Apache AGE Cypher queries use SET LOCAL app.current_tenant so
  # those connections MUST go directly to postgres:5432, NOT via
  # PgBouncer. Use PGBOUNCER_URL only for standard Drizzle queries.
  #
  # Max connections: 1000 clients → 25 server connections per pool
  # ═══════════════════════════════════════════════════════════════
  pgbouncer:
    image: pgbouncer/pgbouncer:1.22.1
    container_name: edusphere-pgbouncer
    environment:
      DATABASES_HOST: postgres
      DATABASES_PORT: 5432
      DATABASES_DBNAME: edusphere
      DATABASES_USER: ${POSTGRES_USER:-edusphere}
      DATABASES_PASSWORD: ${POSTGRES_PASSWORD:-edusphere_dev_password}
      PGBOUNCER_POOL_MODE: transaction
      PGBOUNCER_MAX_CLIENT_CONN: 1000
      PGBOUNCER_DEFAULT_POOL_SIZE: 25
      PGBOUNCER_SERVER_IDLE_TIMEOUT: 60
      # Reject unauthenticated connections
      PGBOUNCER_AUTH_TYPE: scram-sha-256
      # Log connections and disconnections for debugging
      PGBOUNCER_LOG_CONNECTIONS: 1
      PGBOUNCER_LOG_DISCONNECTIONS: 1
    ports:
      - "5433:5432"
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -h 127.0.0.1 -p 5432 -U ${POSTGRES_USER:-edusphere}",
        ]
      interval: 5s
      timeout: 3s
      retries: 5
    mem_limit: 128m
    mem_reservation: 64m
    restart: unless-stopped
    networks:
      - edusphere

  # ═══════════════════════════════════════════════════════════════
  # Redis
  # ═══════════════════════════════════════════════════════════════
  redis:
    image: redis:8.6.0-alpine
    container_name: edusphere-redis
    command: redis-server --requirepass edusphere_redis_password
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    mem_limit: 256m
    mem_reservation: 64m
    networks:
      - edusphere

  # ═══════════════════════════════════════════════════════════════
  # NATS JetStream
  # ═══════════════════════════════════════════════════════════════
  nats:
    image: nats:2-alpine
    container_name: edusphere-nats
    command: ["-js", "-m", "8222", "--config", "/etc/nats/jetstream.conf"]
    ports:
      - "4222:4222"
      - "8222:8222"
    volumes:
      - ./infrastructure/nats/jetstream.conf:/etc/nats/jetstream.conf:ro
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost 4222 || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 5
    mem_limit: 256m
    mem_reservation: 64m
    networks:
      - edusphere

  # ═══════════════════════════════════════════════════════════════
  # Keycloak (Port 8080)
  # ═══════════════════════════════════════════════════════════════
  keycloak:
    image: quay.io/keycloak/keycloak:26.5.3
    container_name: edusphere-keycloak
    environment:
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin123
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://postgres:5432/edusphere
      KC_DB_USERNAME: edusphere
      KC_DB_PASSWORD: edusphere_dev_password
      KC_HOSTNAME_STRICT: false
      KC_HTTP_ENABLED: true
    command: start-dev --import-realm
    ports:
      - "8080:8080"
    volumes:
      - ./infrastructure/docker/keycloak-realm.json:/opt/keycloak/data/import/edusphere-realm.json
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "exec 3<>/dev/tcp/localhost/8080 && echo -e 'GET /health/ready HTTP/1.1\\r\\nHost: localhost\\r\\n\\r\\n' >&3 && cat <&3 | grep -q '200 OK'",
        ]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 60s
    mem_limit: 1g
    mem_reservation: 256m
    networks:
      - edusphere

  # ═══════════════════════════════════════════════════════════════
  # Jaeger - Distributed Tracing (OTLP HTTP on 4318, UI on 16686)
  # Accepts OTLP HTTP traces/metrics from all subgraphs via port 4318.
  # ═══════════════════════════════════════════════════════════════
  jaeger:
    image: jaegertracing/jaeger:2.15.1
    container_name: edusphere-jaeger
    # Jaeger v2: image renamed jaegertracing/all-in-one → jaegertracing/jaeger
    # OTLP enabled by default — COLLECTOR_OTLP_ENABLED removed
    # Port 14268 (Jaeger Thrift HTTP) removed in v2; use OTLP on 4318 instead
    ports:
      - "16686:16686" # Jaeger UI
      - "4317:4317" # OTLP gRPC
      - "4318:4318" # OTLP HTTP (used by @edusphere/telemetry)
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:16686/ || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
    mem_limit: 512m
    mem_reservation: 128m
    networks:
      - edusphere
    restart: unless-stopped

  # ═══════════════════════════════════════════════════════════════
  # Core Subgraph (Port 4001)
  # ═══════════════════════════════════════════════════════════════
  subgraph-core:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: edusphere-subgraph-core
    working_dir: /app/apps/subgraph-core
    command: pnpm dev
    environment:
      - NODE_ENV=development
      - PORT=4001
      - DATABASE_URL=postgresql://edusphere:edusphere_dev_password@postgres:5432/edusphere
      - KEYCLOAK_URL=http://keycloak:8080
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://jaeger:4318
      - OTEL_SERVICE_NAME=subgraph-core
      - NODE_OPTIONS=--max-old-space-size=384
    ports:
      - "4001:4001"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/apps/subgraph-core/node_modules
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost 4001 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    mem_limit: 512m
    mem_reservation: 256m
    depends_on:
      postgres:
        condition: service_healthy
      nats:
        condition: service_healthy
      keycloak:
        condition: service_healthy
      jaeger:
        condition: service_healthy
    networks:
      - edusphere

  # ═══════════════════════════════════════════════════════════════
  # Content Subgraph (Port 4002)
  # ═══════════════════════════════════════════════════════════════
  subgraph-content:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: edusphere-subgraph-content
    working_dir: /app/apps/subgraph-content
    command: pnpm dev
    environment:
      - NODE_ENV=development
      - PORT=4002
      - DATABASE_URL=postgresql://edusphere:edusphere_dev_password@postgres:5432/edusphere
      - KEYCLOAK_URL=http://keycloak:8080
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://jaeger:4318
      - OTEL_SERVICE_NAME=subgraph-content
      - NODE_OPTIONS=--max-old-space-size=384
    ports:
      - "4002:4002"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/apps/subgraph-content/node_modules
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost 4002 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    mem_limit: 512m
    mem_reservation: 256m
    depends_on:
      postgres:
        condition: service_healthy
      nats:
        condition: service_healthy
      keycloak:
        condition: service_healthy
      jaeger:
        condition: service_healthy
    networks:
      - edusphere

  # ═══════════════════════════════════════════════════════════════
  # Annotation Subgraph (Port 4003)
  # ═══════════════════════════════════════════════════════════════
  subgraph-annotation:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: edusphere-subgraph-annotation
    working_dir: /app/apps/subgraph-annotation
    command: pnpm dev
    environment:
      - NODE_ENV=development
      - PORT=4003
      - DATABASE_URL=postgresql://edusphere:edusphere_dev_password@postgres:5432/edusphere
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://jaeger:4318
      - OTEL_SERVICE_NAME=subgraph-annotation
      - NODE_OPTIONS=--max-old-space-size=384
    ports:
      - "4003:4003"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/apps/subgraph-annotation/node_modules
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost 4003 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    mem_limit: 512m
    mem_reservation: 256m
    depends_on:
      postgres:
        condition: service_healthy
      nats:
        condition: service_healthy
      jaeger:
        condition: service_healthy
    networks:
      - edusphere

  # ═══════════════════════════════════════════════════════════════
  # Collaboration Subgraph (Port 4004)
  # ═══════════════════════════════════════════════════════════════
  subgraph-collaboration:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: edusphere-subgraph-collaboration
    working_dir: /app/apps/subgraph-collaboration
    command: pnpm dev
    environment:
      - NODE_ENV=development
      - PORT=4004
      - DATABASE_URL=postgresql://edusphere:edusphere_dev_password@postgres:5432/edusphere
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://jaeger:4318
      - OTEL_SERVICE_NAME=subgraph-collaboration
      - NODE_OPTIONS=--max-old-space-size=384
    ports:
      - "4004:4004"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/apps/subgraph-collaboration/node_modules
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost 4004 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    mem_limit: 512m
    mem_reservation: 256m
    depends_on:
      postgres:
        condition: service_healthy
      nats:
        condition: service_healthy
      jaeger:
        condition: service_healthy
    networks:
      - edusphere

  # ═══════════════════════════════════════════════════════════════
  # Agent Subgraph (Port 4005)
  # ═══════════════════════════════════════════════════════════════
  subgraph-agent:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: edusphere-subgraph-agent
    working_dir: /app/apps/subgraph-agent
    command: pnpm dev
    environment:
      - NODE_ENV=development
      - PORT=4005
      - DATABASE_URL=postgresql://edusphere:edusphere_dev_password@postgres:5432/edusphere
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://jaeger:4318
      - OTEL_SERVICE_NAME=subgraph-agent
      - NODE_OPTIONS=--max-old-space-size=768
    ports:
      - "4005:4005"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/apps/subgraph-agent/node_modules
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost 4005 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    mem_limit: 1g
    mem_reservation: 512m
    depends_on:
      postgres:
        condition: service_healthy
      nats:
        condition: service_healthy
      jaeger:
        condition: service_healthy
    networks:
      - edusphere

  # ═══════════════════════════════════════════════════════════════
  # Knowledge Subgraph (Port 4006) - pgvector
  # ═══════════════════════════════════════════════════════════════
  subgraph-knowledge:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: edusphere-subgraph-knowledge
    working_dir: /app/apps/subgraph-knowledge
    command: pnpm dev
    environment:
      - NODE_ENV=development
      - PORT=4006
      - DATABASE_URL=postgresql://edusphere:edusphere_dev_password@postgres:5432/edusphere
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://jaeger:4318
      - OTEL_SERVICE_NAME=subgraph-knowledge
      - NODE_OPTIONS=--max-old-space-size=768
    ports:
      - "4006:4006"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/apps/subgraph-knowledge/node_modules
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost 4006 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    mem_limit: 1g
    mem_reservation: 512m
    depends_on:
      postgres:
        condition: service_healthy
      nats:
        condition: service_healthy
      jaeger:
        condition: service_healthy
    networks:
      - edusphere

  # ═══════════════════════════════════════════════════════════════
  # Gateway (Port 4000) - Hive Gateway v2.7
  # ═══════════════════════════════════════════════════════════════
  gateway:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: edusphere-gateway
    working_dir: /app/apps/gateway
    command: pnpm dev
    environment:
      - NODE_ENV=development
      - PORT=4000
      - SUBGRAPH_CORE_URL=http://subgraph-core:4001/graphql
      - SUBGRAPH_CONTENT_URL=http://subgraph-content:4002/graphql
      - SUBGRAPH_ANNOTATION_URL=http://subgraph-annotation:4003/graphql
      - SUBGRAPH_COLLABORATION_URL=http://subgraph-collaboration:4004/graphql
      - SUBGRAPH_AGENT_URL=http://subgraph-agent:4005/graphql
      - SUBGRAPH_KNOWLEDGE_URL=http://subgraph-knowledge:4006/graphql
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://jaeger:4318
      - OTEL_SERVICE_NAME=gateway
      - NODE_OPTIONS=--max-old-space-size=384
    ports:
      - "4000:4000"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/apps/gateway/node_modules
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost 4000 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    mem_limit: 512m
    mem_reservation: 256m
    depends_on:
      subgraph-core:
        condition: service_healthy
      subgraph-content:
        condition: service_healthy
      subgraph-annotation:
        condition: service_healthy
      subgraph-collaboration:
        condition: service_healthy
      subgraph-agent:
        condition: service_healthy
      subgraph-knowledge:
        condition: service_healthy
      jaeger:
        condition: service_healthy
    networks:
      - edusphere

  # ═══════════════════════════════════════════════════════════════
  # Prometheus - Metrics collection
  # Start with: docker-compose -f docker-compose.dev.yml --profile monitoring up -d
  # ═══════════════════════════════════════════════════════════════
  prometheus:
    image: prom/prometheus:v3.2.1
    container_name: edusphere-prometheus-dev
    volumes:
      - ./infrastructure/monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./infrastructure/monitoring/rules:/etc/prometheus/rules:ro
      - prometheus_data:/prometheus
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
      - "--web.console.libraries=/etc/prometheus/console_libraries"
      - "--web.console.templates=/etc/prometheus/consoles"
      - "--web.enable-lifecycle"
    ports:
      - "9090:9090"
    networks:
      - edusphere
    profiles:
      - monitoring

  # ═══════════════════════════════════════════════════════════════
  # Grafana - Dashboard visualization
  # UI available at http://localhost:3001  (admin / edusphere)
  # ═══════════════════════════════════════════════════════════════
  grafana:
    image: grafana/grafana:12.3.2
    container_name: edusphere-grafana-dev
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=edusphere
      - GF_PATHS_PROVISIONING=/etc/grafana/provisioning
    volumes:
      - ./infrastructure/monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./infrastructure/monitoring/grafana/dashboards:/etc/grafana/dashboards:ro
      - grafana_data:/var/lib/grafana
    ports:
      - "3001:3000"
    depends_on:
      - prometheus
    networks:
      - edusphere
    profiles:
      - monitoring

networks:
  edusphere:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
  prometheus_data:
  grafana_data:

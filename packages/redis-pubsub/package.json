{
  "name": "@edusphere/redis-pubsub",
  "version": "0.1.0",
  "private": true,
  "description": "Redis PubSub wrapper for real-time events",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "ioredis": "^5.4.2"
  },
  "devDependencies": {
    "@edusphere/tsconfig": "workspace:*",
    "@types/node": "^22.10.5",
    "@vitest/coverage-v8": "^4.0.18",
    "typescript": "^6.0.3",
    "vitest": "^4.0.18"
  }
}

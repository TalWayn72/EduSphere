#!/bin/sh
command -v git-lfs >/dev/null 2>&1 || { printf >&2 "\n%s\n\n" "This repository is configured for Git LFS but 'git-lfs' was not found on your path. If you no longer wish to use Git LFS, remove this hook by deleting the 'post-merge' file in the hooks directory (set by 'core.hookspath'; usually '.git/hooks')."; exit 2; }
git lfs post-merge "$@"

# ── Iron Rule: Service Restoration Guard ──────────────────────────────────────
# After every merge, verify services are still running.
if [ -f "scripts/verify-services.sh" ]; then
  echo ""
  echo "[post-merge] Running service verification (Iron Rule)..."
  bash scripts/verify-services.sh &
fi
